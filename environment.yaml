dependencies:
  - python=3.10

  # RAPIDS 24.06 core (CUDA-12.x compatible)
  - cudf=24.06
  - cuml=24.06
  - cugraph=24.06
  - cupy

  # Core scientific + ML stack
  - numpy=1.26.*
  - pandas=2.2.*
  - scikit-learn=1.5.*
  - hdbscan
  - umap-learn>=0.5.6   # ✅ added

  # GPU PyTorch stack
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - sentence-transformers=3.0.*

  # Utilities and dev tools
  - tqdm
  - psutil
  - pyyaml
  - joblib
  - jupyterlab
  - black
  - flake8
  - mypy
  - pytest
  - matplotlib >=3.8

  - pip:
      - rapidfuzz>=3.6.1
